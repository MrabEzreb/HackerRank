package com.ezrebclan.practice.algorithms

class Palendromes {

  def main(args: Array[String]): Unit = {
    
  }
  
  def isEven(word: String): Boolean = {
    if(word.length() % 2 == 0) {
      true
    } else {
      false
    }
  }
}